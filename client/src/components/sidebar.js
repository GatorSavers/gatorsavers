import React from 'react';

const SideBar = () => (
  <section id="sidebar">
    <section id="intro">
      <header>
        <h2>Budget Overview</h2>
        <p>Login to see your budget lists and favorites!</p>
      </header>
    </section>
  </section>
);

export default SideBar;